<article >
	<h3>	
		<%=  @post.title %>
	</h3>
	<h4>
		<%= @post.created_at.strftime("%d/%m/%Y") %>
	</h4>

	<div class="post">
		<%= @post.text.html_safe %>
	</div>

	<%= link_to 'Back', posts_path %>
</article>